# Separate socket service is no longer needed after the merged-server setup.
# Keep this only if you intentionally run the legacy standalone socket server.
NODE_ENV=production
PORT=10000

# Optional later
# REDIS_URL=
